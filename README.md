# members-only
Authentication App using express, ejs, express-validator, passport and bcrypt

Add delete button next to each message that is only visible if user membership is admin
It will delete message by message id which must be passed in via params

Add input validation including custom second password confirmation field