# members-only
Authentication App using express, ejs, express-validator, passport and bcrypt

Right now it never makes it to the homepage for unauthenticated users (see what can be done about authmiddleware)